import math

A,B,C=map(float,input().split())
delta=B*B - 4*A*C



if delta<0 or A==0:
    print("Impossivel calcular")
else:
    x = (-B + math.sqrt(delta)) / (2 * A)
    x2 = (-B - math.sqrt(delta)) / (2 * A)
    print("R1 = %.5f" % x)
    print("R2 = %.5f" % x2)
